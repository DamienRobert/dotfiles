#!/bin/sh
. "$HOME/.initenv"
mylog "session: .gnomerc read" 

. "$HOME/.xprofile"

# à l'imb, le gnome-rc est execute, je dois donc lancer gnome-session

if [ x$HOSTNAME = xFleurBleue ]; then
  exec gnome-session
fi
