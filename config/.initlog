#vim: ft=sh
if [ -z "$MYTMP" ]; then
  MYTMP=/var/tmp
  [ -d "$HOME/tmp" ] && MYTMP="$HOME/tmp"
fi

LOGDIR="$MYTMP/mylogs"
mkdir -p "$LOGDIR"

mylog() {
  file=$1; shift;
  LOGFILE="$LOGDIR/$file"
  [ -f "$LOGFILE" ] || touch $LOGFILE
  echo "@`date --rfc-3339=seconds` - $@" >> $LOGFILE
}
