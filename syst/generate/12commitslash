#!/bin/zsh

DEFAULT="$HOME/mine/syst/@comps/$MYHOSTNAME/slash"
DIR=${1-$DEFAULT}

echo "# Overwrite sys config files by the values in `pwd`"
echo "# Are you sure? Press a key to continue"
read

$(dirname $0)/copydir -u root $DIR /
