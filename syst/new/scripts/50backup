#!/bin/zsh

case $FS in
	btrfs)
		#subvolume
		if [[ -d /mnt/slash ]]; then
			#/ is a subvolume of a partition that is mounted in /mnt/
			sudo btrfs subvolume snapshot / /mnt/slash-init
		else
			sudo btrfs subvolume snapshot / /slash-init
		fi
		;;
esac
