#ffmpeg:
LD_LIBRARY_PATH=~/lib/local ~/bin/local/fetchmail

#w3m:
#!/bin/sh
[ -x /usr/bin/w3m ] && exec /usr/bin/w3m "$@"
exec $HOME/bin/local/w3m-binary "$@"

#ruby:
#!/bin/sh
#libc=$(readlink /lib/libc.so.6)
#
#if [ "$libc" = "libc-2.13.so" ]; then
#  exec /usr/bin/ruby "$@"
#else
  exec /home/imb/drobert/opt/bin/ruby "$@"
#fi

#mutt:
#!/bin/sh
MAIL="$HOME/Mail/Inbox/" /usr/bin/mutt "$@"

